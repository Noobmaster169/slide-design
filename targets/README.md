# Targets

A figure is a standalone SVG, so it drops into whatever you are making. See `docs/embedding.md`.

`slide-frame.html` is a minimal example of inlining a figure at slide size with a title over it,
for people who want a deck without adopting a deck framework.
